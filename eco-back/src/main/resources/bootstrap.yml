configserver:
  host: localhost

spring:
  application:
    name: product-service
  cloud:
    config:
      uri: http://${configserver.host}:8888 
   
---

spring:
  profiles: staging

configserver:
  host: config-server

---

spring:
  profiles: prod

configserver:
  host: config-server
