spring:
  application:
    name: product-service
  cloud:
    config:
      uri: http://localhost:8888 

---

spring:
  profiles: staging
  application:
    name: product-service
  cloud:
    config:
      uri: http://config-server:8888 
      
---

spring:
  profiles: prod
  application:
    name: product-service
  cloud:
    config:
      uri: http://config-server:8888 