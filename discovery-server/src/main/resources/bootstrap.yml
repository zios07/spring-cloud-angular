spring:
  application:
    name: discovery-server
  cloud:
    config:
      uri: http://localhost:8888 
   
---

spring:
  profiles: staging

spring:
  application:
    name: discovery-server
  cloud:
    config:
      uri: http://config-server:8888 

---

spring:
  profiles: prod

spring:
  application:
    name: discovery-server
  cloud:
    config:
      uri: http://config-server:8888
