server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/Desktop/ecommerce-app/app-v3/ecommerce-app
          search-paths:
          - config-repo

---
server:
  port: 8888
  
spring:
  profiles: staging, prod
  cloud:
    config:
      label: cart-service
      server:
        git:
          uri: https://github.com/zios07/ecommerce-app.git
          username: zios07
          password:
          force-pull: true
          search-paths:
          - config-repo