server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: file://${user.dir}/..
          search-paths:
          - config-repo

---
spring:
  profiles: staging, prod
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zios07/ecommerce-app.git
          username: zios07
          password:
          search-paths:
          - config-repo
