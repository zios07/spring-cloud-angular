server:
  port: 9902

spring:
  data:
    mongodb:
      username: root
      password: root
      host: localhost
      database: test

  rabbitmq:
    host: localhost
    port: 5672
    password: root
    username: root
    virtual-host: /

queue:
  attachment:
    name: AttachmentQueue
